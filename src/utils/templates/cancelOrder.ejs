<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>
        <%= mailTitle %>
    </title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding: 20px;
        }

        .container {
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            max-width: 600px;
            margin: auto;
        }

        .header {
            text-align: center;
            color: #333;
        }

        .order-details {
            margin-top: 20px;
        }

        .product {
            border-bottom: 1px solid #ddd;
            padding: 10px 0;
        }

        .footer {
            text-align: center;
            margin-top: 20px;
            font-size: 12px;
            color: #777;
        }
    </style>
</head>

<body>
    <div class="container">
        <h2 class="header">
            <%= mailTitle %>
        </h2>
        <p>Dear <%= customerName %>,</p>
        <p>We regret to inform you that your order <strong>#<%= orderId %></strong> has been cancelled.</p>
        <p><strong>Cancellation Reason:</strong>
            <%= cancellationReason %>
        </p>
        <p><strong>Refund Amount:</strong> ₹<%= refundAmount.toFixed(2) %>
        </p>

        <h3>Order Details:</h3>
        <div class="order-details">
            <% products.forEach(function(product) { %>
                <div class="product">
                    <img src="<%= product.productImage %>" alt="<%= product.productName %>"
                        style="width: 100px; height: 100px; object-fit: cover;">
                    <p><strong>Product:</strong>
                        <%= product.productName %>
                    </p>
                    <p><strong>Variant:</strong>
                        <%= product.variantName %>
                    </p>
                    <p><strong>Price:</strong> ₹<%= product.price %>
                    </p>
                    <p><strong>Quantity:</strong>
                        <%= product.quantity %>
                    </p>
                </div>
                <% }); %>
        </div>

        <p>If you have any questions, please contact our support team.</p>
        <p>Thank you for shopping with <strong>
                <%= appName %>
            </strong>.</p>

        <div class="footer">
            &copy; <%= year %>
                <%= appName %>. All Rights Reserved.
        </div>
    </div>
</body>

</html>